<form method="post" action="{{ path('house_delete', {'id': house.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ house.id) }}">
    <button class="btn">Delete</button>
</form>
